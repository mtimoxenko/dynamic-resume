# include:
#   - local: '/pipelines/terraform.yml'
variables:
  TF_IMAGE: hashicorp/terraform:latest
  TF_PATH: IaC/provisioning

terraform-plan:
  image: ${TF_IMAGE}
  before_script:
  - cd ${TF_PATH}
  script:
    - terraform init
    - terraform plan
